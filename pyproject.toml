[project]
name = "youtube-downloader-cli"
version = "0.1.0"
description = "CLI Downloader YouTube"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "rich>=13.0.0",
    "typer[all]>=0.12.0",
    "yt-dlp>=2024.0.0",
]

[project.scripts]
ytdl = "ytdl:main"
